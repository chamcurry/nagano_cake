<div class="container">
  <div class="row">
    <div class="col">
      <h3>ジャンル編集</h3>
     <%= form_with model: @genres  do |f| %>
      <span class="form_group">
        ジャンル名 <%= f.text_field :name %> <%=f.submit '変更を保存',class: 'btn btn-success' %>
      </span>
     <% end %>
    </div>
  </div>
</div>